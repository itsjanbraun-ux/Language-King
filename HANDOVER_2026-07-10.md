# Language King - Handover 2026-07-10

## Aktueller Projektstand

- Projektpfad: `C:\Users\jbr\Documents\GitHub\Privat\Vokabelspiel`
- Hauptdatei: `index.html`
- App bleibt eine Single-File-HTML-App ohne Build-Step.
- Aktueller Branch: `main`
- Letzter gepushter Commit bei dieser Handover-Aktualisierung: `133ad16 Add profile learn progress reset`
- GitHub Pages wurde zuletzt mit Cache-Parameter geprueft:
  - `https://itsjanbraun-ux.github.io/Language-King/?v=2026-07-14-learn-reset`

## Wichtige Regeln fuer naechste Anpassungen

- JavaScript im Projekt konservativ schreiben: `var`, `function`, keine Arrow Functions, keine Template Literals.
- Umlaute in JavaScript als Unicode-Escapes schreiben.
- Sichtbare UI-Aenderungen nicht nur per Syntax pruefen: wenn moeglich mit Browser/Screenshot pruefen.
- Exact-Flow-Smoke-Tests fuer neue Funktionen nutzen.
- GitHub Pages nach Push immer mit frischem Cache-Parameter testen.
- Bei Bild-Wort/Lernen auf echte Profil- und Reload-Flows achten, nicht nur auf Syntax.

## Heute abgeschlossen

### Bild-Wort Datenstand

- Supabase `vocab_items` wurde per `supabase_image_mvp_seed.sql` auf 25 englische Bildwoerter gebracht.
- Oeffentliche Supabase-REST-Pruefung ergab danach:
  - Status `200`
  - `Content-Range 0-24/25`
  - Count `25`
- App und Supabase haben damit denselben 25-Wort-MVP-Stand.

### Bild-Wort Rundenauswahl

- Fehler behoben: Bild-Wort nutzte zuerst eine vorher zufaellig gekuerzte Lernmenue-Auswahl.
- `startLearnType("image")` nutzt jetzt `getFilt()` als vollstaendige Bildbasis.
- `restartImageLearn()` nutzt ebenfalls `getFilt()` als vollstaendige Bildbasis.
- Reproduzierter Logikfall:
  - alter Pfad: `18`
  - neuer Pfad: `25`

### Lernrunde beenden

- Im Lernscreen wurde ein Button `Fertig` neben `Abbrechen` ergaenzt.
- Zweck: Nach einem gespeicherten `Kann ich`/`Nochmal ueben` kann die Runde regulaer beendet werden, ohne alle Karten durchzuklicken.
- Wichtiges Verhalten:
  - Fortschritt wird bei `Kann ich` oder `Nochmal ueben` gespeichert.
  - `Fertig` bewertet keine offene Karte, sondern beendet die Runde und zeigt die Zusammenfassung.
  - `Abbrechen` bleibt als harter Ausstieg mit Warnung erhalten.

### Bild-Lernfortschritt Anzeige

- Startscreen-Anzeige wurde von unklarem `Bilder-Englisch: X gelernt` auf bildspezifisches `X / total` umgestellt.
- Beispiel:
  - `Bilder-Englisch: 1 / 25 gelernt`
  - `Bilder-Englisch: 25 / 25 gelernt`
- Neue Hilfsfunktionen:
  - `getImageProgressCards(lang)`
  - `countImageProgressTotal(lang)`
  - `countKnownImageStats(lang)`
- Bild-Wort-Zusammenfassung nutzt jetzt ebenfalls `countKnownImageStats(cLang)`.

### Nachtrag 2026-07-14: Bild-Lernfortschritt Sync und Reset

- Fehlerbild aus echtem Ben-Test:
  - Nach 1 bis 2 gelernten Bildwoertern zeigte die Startscreen-Anzeige weiter `Bilder-Englisch: 25 / 25 gelernt`.
  - Cache-Buster und neu gepushte Versionen aenderten das Verhalten zunaechst nicht.
- Code-Fixes:
  - `loadLearnStatsFromDB()` baut `learnStats` fuer Supabase-Profile nach erfolgreichem DB-Load komplett neu aus `profile_word_stats` auf.
  - `syncCurrentProfileAfterLoad()` aktualisiert das aktive Profil `cp`, wenn `profs` nach einem Supabase-Load ersetzt wird.
  - `mapDbProfToLocal()` uebernimmt fuer Supabase-Profile keine alten lokalen `learnStats` mehr.
- Ergebnis der Analyse:
  - Wenn danach weiterhin `25 / 25` erscheint, sind die 25 bekannten Bildwoerter sehr wahrscheinlich bereits in Supabase `profile_word_stats` gespeichert.
- Elternbereich erweitert:
  - Neuer Button `Lernstand reset` pro Profil.
  - Erstklick schaltet auf `Lernstand wirklich reset?`, Zweitklick fuehrt aus.
  - Reset leert lokal `learnStats` fuer dieses Profil.
  - Reset loescht in Supabase `profile_word_stats` fuer genau dieses Profil per `delete().eq("profile_id", p._dbid)`.
  - Reset setzt nicht die Vokabeln, Bilddaten, Vokabelsets, Klasse, Avatar, PIN, XP oder Historie zurueck.
- Ben-Test fuer morgen:
  1. GitHub Pages mit `?v=2026-07-14-learn-reset` oeffnen.
  2. Eltern-Login.
  3. `Profile verwalten`.
  4. Bei Ben `Lernstand reset` zweimal klicken.
  5. Ben neu oeffnen.
  6. Erwartet: `Bilder-Englisch: 0 / 25 gelernt`.
  7. Zwei Bild-Wort-Karten mit `Kann ich` markieren und `Fertig` klicken.
  8. Erwartet: `Bilder-Englisch: 2 / 25 gelernt`.
- Falls der Reset meldet, dass Supabase nicht zurueckgesetzt wurde:
  - Wahrscheinliche Ursache: RLS blockiert `delete` auf `profile_word_stats`.
  - Naechster Fix: Reset nicht per Delete, sondern per Upsert/Update auf `correct_count=0` und `wrong_count=0` fuer die 25 Bildwoerter bauen oder RLS-Policy pruefen.

## Zuletzt getestete Checks

- JavaScript aus `index.html` per `node --check` geprueft.
- HTML-Struktur geprueft:
  - `282` oeffnende `div`
  - `282` schliessende `div`
  - `9` Screens
  - balanced
- Isolierter Logiktest fuer Bildrunden:
  - Startpfad alter Pool `18`, neuer Pool `25`
  - Neustartpfad alter Pool `18`, neuer Pool `25`
- Isolierter Logiktest fuer Bildfortschritt:
  - total `25`
  - zero `0`
  - one `1`
  - all `25`
- GitHub Raw und GitHub Pages wurden nach Pushes mit frischen Cache-Parametern geprueft.
- Nachtrag 2026-07-14:
  - Inline-JavaScript aus `index.html` per `new Function(...)` geprueft.
  - HTML-Struktur geprueft:
    - `282` oeffnende `div`
    - `282` schliessende `div`
    - `9` Screens
    - balanced
  - Logik-Smokes:
    - 25 alte lokale Bild-Stats plus 1 echter DB-Stat ergibt nach Reload `1 / 25`, nicht `25 / 25`.
    - Aktives Profil mit altem lokalem `25 / 25` wird nach Supabase-Reload auf frische `2 / 25` Stats umgehaengt.
    - Supabase-Profilmapping uebernimmt keine alten lokalen `learnStats` mehr.
    - Reset-Implementierung enthaelt Button, lokale Leerung und Supabase-Delete auf `profile_word_stats`.
  - GitHub Raw geprueft:
    - `HasResetButton: True`
    - `HasResetFunction: True`
    - `HasDeleteStats: True`
  - GitHub Pages HEAD mit `?v=2026-07-14-learn-reset` ergab Status `200`.

## Bekannte offene Punkte

- Lernfortschritt-Sync ist code-seitig vorhanden und `profile_word_stats` existiert in Supabase.
- Noch offen: echter Ben-Flow nach `Lernstand reset` verifizieren:
  1. Ben-Lernstand im Elternbereich zuruecksetzen.
  2. Ben neu oeffnen und `0 / 25` pruefen.
  3. Zwei Bild-Wort-Karten mit `Kann ich` speichern.
  4. `Fertig` klicken.
  5. App neu laden oder in anderem Browser/Profil oeffnen.
  6. Ben erneut oeffnen.
  7. Pruefen, ob `Bilder-Englisch: 2 / 25 gelernt` wieder geladen wird.
- Bildwortbestand auf 50+ erweitern ist bewusst auf spaeter verschoben.
- Echter Bild-Upload ist ebenfalls spaeterer groesserer Block mit Supabase Storage und RLS.
- Memory-Spiel Phase 2 ist weiterhin Platzhalter.
- Eltern-/Profilbereich kann in einer separaten QA-Runde weiter geglaettet werden.

## Empfohlene naechste Schritte

1. Ben-Lernstand per Elternbereich zuruecksetzen und danach den echten `0 / 25` -> `2 / 25` Flow live verifizieren.
2. Danach entscheiden:
   - Stabilitaetsblock: Eltern-/Profilbereich QA.
   - Produktblock: Memory-Spiel Phase 2.
   - Datenblock spaeter: Bildwoerter auf 50+ erweitern.
3. Wenn der Reset oder Sync im Live-Test nicht passt, zuerst Supabase-Auth/RLS fuer `profile_word_stats` pruefen.

## Letzte relevante Commits

- `133ad16 Add profile learn progress reset`
- `39af486 Ignore stale local learn stats for synced profiles`
- `8cf5dea Fix synced image progress count`
- `214f1b1 Show image learning progress total`
- `9651729 Add learn round finish button`
- `63f7789 Update index.html`
- `74a25ee Update index.html`
- `00c21f7 Add July 9 handover`
- `f91503d Add image vocab import help`
